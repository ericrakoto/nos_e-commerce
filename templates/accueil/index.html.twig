<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <title>{% block title %}{% endblock %}</title>
        {% block stylesheets %}
          <link rel="stylesheet" href="{{asset('css/bootstrap.min.css')}}" type="text/css" >
          <link rel="stylesheet" href="{{asset('css/eric.css')}}" type="text/css" >
           <link rel="stylesheet" href="{{asset('css/fontawesome.css')}}" type="text/css" >
           <link rel="stylesheet" href="{{asset('css/all.min.css')}}" type="text/css">

           
        {% endblock %}
    </head>
    <body>
      {% if app.user != null %}
      <header class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
      <p class="h5 my-0 mr-md-auto fw-normal">NosyTech</p>
      <nav class="my-2 my-md-0 mr-md-3">
      <a class="btn btn-outline-primary" href="/accueil">Accueil</a>
      <a class="btn btn-outline-primary" href="/admin">Admin</a>
      
      <a  class="btn btn-outline-primary" href="/adm/panier"><span class="panier"><i class="fas fa-cart-plus fa-lg mr-2"></i>
        Panier <button class="tot">{{totalQuantite}}</button></span></a>
      
      <a class="btn btn-outline-secondary" href="/logout">deconnexion</a>
      </nav>
      </header>
      {% else %}
      <header class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
      <p class="h5 my-0 mr-md-auto fw-normal">NosyTech</p>
      <nav class="my-2 my-md-0 mr-md-3">
      <a class="btn-tsindrio" href="/register">s'enregistrer</a>
      </nav>
      <a class="btn-tsindrio" href="/login">login</a>
      </header>
      {% endif %}
      {% block body %}
      <main role="main" class="container">

        <div id="myCarousel" class="carousel slide" data-ride="carousel">
    <ol class="carousel-indicators">
      <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
      <li data-target="#myCarousel" data-slide-to="1"></li>
      <li data-target="#myCarousel" data-slide-to="2"></li>
    </ol>
    <div class="carousel-inner">
      <div class="carousel-item active">
        <img style="width: 1200px;" src="{{asset('main/fond.jpg')}}">

        <div class="container">
          <div class="carousel-caption text-left">
            <h1>NosyTech</h1>
            <p>Ny orinasa NosyTech dia orinasa manana trai-kefa amin'ny tontolon'ny technology</p>

          </div>
        </div>
      </div>
      <div class="carousel-item">
        <img style="width: 1200px;" src="{{asset('main/fond1.jpg')}}">

        <div class="container">
          <div class="carousel-caption">
            <h1>NosyTech</h1>
            <p>Ny orinasa NosyTech dia afaka manoro anao amin'ny rehetra momban'ny technology</p>

          </div>
        </div>
      </div>
      <div class="carousel-item">
        <img style="width: 1200px;" src="{{asset('main/fond3.jpg')}}">

        <div class="container">
          <div class="carousel-caption text-right">
            <h1>NosyTech</h1>
            <p>Ny orinasa NosyTech dia afaka manatanteraka ireo vina rehetra amin'izao fotoana izao</p>

          </div>
        </div>
      </div>
    </div>
    <a class="carousel-control-prev" href="#myCarousel" role="button" data-slide="prev">
      <span class="carousel-control-prev-icon" aria-hidden="true"></span>
      <span class="visually-hidden">Previous</span>
    </a>
    <a class="carousel-control-next" href="#myCarousel" role="button" data-slide="next">
      <span class="carousel-control-next-icon" aria-hidden="true"></span>
      <span class="visually-hidden">Next</span>
    </a>
  </div>

      <h1>Entana</h1>

      <div class="row row-cols-1 row-cols-md-3 mb-3 text-center">
      {% for entana in entanas %}
      
      <div class="col-12 col-lg-3">
      <div class="card mb-4 shadow-sm">
      <div class="card-header">
      <h4 class="my-0 fw-normal">{{ entana.titreProduit }}</h4>
      </div>
      <div class="card-body">
      <h1 class="card-title pricing-card-title">{{ entana.vidiny }} Ar <small class="text-muted"></small></h1>
      <div  class="entana" style="width: 165px;height: 165px;align-items: center;margin: auto;">
      <img style="height:165px;width: 165px; margin:auto;" src="uploads/{{entana.sary}}">
      </div>
      <a class="sfm" href="{{path('panier_gestion',{'id': entana.id})}}">
          <button type="button" class="btn-vidio" style="padding: 0.5rem 1rem;
          font-size: 1.25rem;
          border-radius: 0.3rem;">
          <i class="fas fa-cart-plus fa-lg mr-2"></i>
        Vidio
      </button>
      </a>
      </div>
      </div>
      </div>
       
      {% endfor %}
      </div> 
      </main>
                 
      {% endblock %}
 </body>
 {% block javascripts %}
                 <script src="{{asset('js/jquery-3.5.1.js')}}"></script>
                 <script src="{{asset('js/jquery-latest.min.js')}}"></script>
                 <script src="{{asset('js/bootstrap.bundle.min.js')}}"></script>
                 <script src="{{asset('js/bootstrap.min.js')}}"></script>
                 <script src="{{asset('js/glm-ajax.js')}}"></script>
                 <script src="{{asset('js/axios.min.js')}}"></script>
                  <script>
                    function onClickBtnVidio(event){
                      event.preventDefault();
                      const tt = document.querySelector('.tot');
                      axios.get(this.href).then(function (response){
                        tt.textContent = response.data.totalQuantite;})}
                    document.querySelectorAll('.sfm, .btn-vidio').forEach(function(link){link.addEventListener('click', onClickBtnVidio);
                    })

                    </script>
            
                {% endblock %}

</html>
